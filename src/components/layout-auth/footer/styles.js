import { Layout } from 'antd';
import styled from 'styled-components/macro';

const { Footer } = Layout;

export const LayoutFooterStyle = styled(Footer)`
  text-align: center;
`;

export default LayoutFooterStyle;
