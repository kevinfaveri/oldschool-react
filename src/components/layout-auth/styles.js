import { Layout } from 'antd';
import styled from 'styled-components/macro';

export const LayoutAuthStyle = styled(Layout)`
  min-height: 100vh !important;

  > * {
    color: #ff6347 !important;
  }
`;

export default LayoutAuthStyle;
